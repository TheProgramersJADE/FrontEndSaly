@* <div class="top-row ps-3 navbar">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Beauty Saly</a>
    </div>
</div> *@
<div class="nav-item px-3">
    <NavLink class="nav-link styled-title">
        <span class="bi bi-person-circle"></span> Beauty Saly
    </NavLink>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="counter">
                <span class="bi bi-person-circle"></span> Usuarios
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/rol/roles">
                <span class="bi bi-shield-fill-check"></span> Roles
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/Servicio/servicios">
                <span class="oi oi-briefcase"></span> Servicios
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/Cita/citas">
                <span class="oi oi-calendar"></span> Citas
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/ServiciosTrabajador/servitrabajador">
                <span class="oi oi-calendar"></span> ServicioTrabajador
            </NavLink>
        </div>
    </nav>
</div>

<style>




    /* Título del menú */
    .styled-title {
        font-family: 'Lilita One', cursive;
        font-size: 1.8rem; /* letra grande */
        font-weight: bold; /* bold */
        color: #111; /* letra negra */
        background-color: #a569bd; /* fondo morado/rosa */
        padding: 0.8rem 1.5rem; /* espacio alrededor */
        border-radius: 12px; /* bordes redondeados */
        display: inline-flex;
        align-items: center;
        text-decoration: none;
        transition: all 0.3s ease;
        box-shadow: 0 3px 6px rgba(0,0,0,0.2);
    }

        /* Icono junto al título */
        .styled-title span {
            margin-right: 10px;
            font-size: 2rem;
        }

        /* Hover efecto flotante */
        .styled-title:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 16px rgba(0,0,0,0.25);
            color: #111; /* siempre negro */
        }





    /* Estilo uniforme para top-row y botones */
    .top-row, .nav-scrollable {
        background-color: #6f2c6f; /* morado oscuro femenino */
        padding: 1rem 1.5rem;
        display: flex;
        justify-content: center;
        align-items: center;
        box-shadow: 0 3px 6px rgba(0,0,0,0.2);
        border-radius: 12px; /* bordes redondeados */
    }

    /* Título */
    .top-row .navbar-brand {
        font-family: 'Lilita One', cursive;
        color: #111; /* letra negra */
        font-weight: bold;
        font-size: 1.8rem;
        letter-spacing: 1px;
    }

    /* Menú lateral / botones */
    .nav-scrollable .nav-link {
        display: inline-flex;
        align-items: center;
            font-family: 'Lilita One', cursive;
            color: #111; /* letra negra */
        background-color: #a569bd; /* rosa/morado suave */
        padding: 0.7rem 1.5rem;
        margin: 0 0.5rem;
        border-radius: 12px;
        font-weight: 500;
        font-size: 1rem;
        transition: all 0.3s ease;
        box-shadow: 0 3px 6px rgba(0,0,0,0.15);
        text-decoration: none;
    }

        /* Iconos */
        .nav-scrollable .nav-link span {
            margin-right: 8px;
            font-size: 1.2rem;
        }

        /* Hover botones */
        .nav-scrollable .nav-link:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 16px rgba(0,0,0,0.25);
            background-color: #af7ac5;
            color: #111; /* siempre negra */
        }

        /* Activo */
        .nav-scrollable .nav-link.active {
            background-color: #9b59b6;
            color: #111;
            transform: translateY(-2px);
            box-shadow: 0 5px 10px rgba(0,0,0,0.2);
        }
</style>
